# IMPLEMENTATION_PLAN

phase / slice 단위 구현 순서와 현재 상태를 정리한다. 완료된 단계와 남은 단계를 함께 관리한다.

## 상태 값

- `not-started`
- `in-progress`
- `blocked`
- `done`
- `deferred`

## Phase 1. `<phase-title>`

- 상태: `not-started`
- 목표: `<goal>`
- 범위:
  - `<scope-item>`
- 완료 기준:
  - `<completion-condition>`
- 검증:
  - `<validation-command-or-scenario>`
- 메모:
  - `<note>`

## 현재 Slice 계획

1. `<step-1>`
2. `<step-2>`
3. `<step-3>`

## 후속 후보

- `<future-slice>`
